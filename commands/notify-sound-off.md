Disable notification sounds by setting `sound: false` in ~/.notify.yaml.

Read ~/.notify.yaml, change the `sound:` line from `true` to `false`, and write it back. If there is no sound line, add `sound: false` after the comment header.
